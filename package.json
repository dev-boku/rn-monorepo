{
  "name": "rn-monorepo",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "turbo": "turbo run",
    "gen": "turbo gen workspace --copy"
  },
  "license": "ISC",
  "engines": {
    "node": "22.20.0",
    "pnpm": "10"
  },
  "packageManager": "pnpm@10.18.0",
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
